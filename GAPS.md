# GAPS.md — open contract gaps from the dry run

These surfaced when the pipeline was traced against a hard prompt (a practitioner primer on reading callout/leader-line figures and segmenting exploded-view parts catalogues; reference case = a ~80-callout binary exploded view; thesis = anchors seed segmentation, masks may be unnecessary). `spec-02-callout-extraction.yaml` keeps this case live.

Apply any fix via the registry + `bash tools/build.sh` — **never** by hand-editing generated files.

| # | Gap | Status | Action |
|---|-----|--------|--------|
| **G1** | `home ≈ target`: reader already lives in the target domain, so the cross-domain bridge metaphor degenerates. | **RESOLVED** | New `R-XREF-04`: `home_anchor` resolves to the nearest adjacent *technique/sub-field*, with a deterministic companion lint (`home_anchor_distinct`) catching self-restatement; `research-perspectives.md` re-aims bridge-builder at prior-art transfer. |
| **G2** | `R-EXPERT-01` (suppress worked examples) collides with mandatory reference-case grounding. | **RESOLVED** | `R-EXPERT-01` narrowed to *procedural* walkthroughs; new `R-EXPERT-03` (running reference case) plus a `reference_case` parameter make the anchor first-class and explicitly not-suppressed. |
| **G3** | No consumer/dual-purpose knob (human reading vs LLM grounding). | **RESOLVED** | Became the dual-output decision: `outputs` param + `R-PROJ-01..06` (IR-first, two projections). |
| **G4** | Verified-vs-inferred provenance not surfaced. | **RESOLVED** | `R-PROJ-05` — provenance (`verified`/`inferred`/`unverified`) + `source_ids` printed inline in both projections; `provenance` axis added to the IR. |
| **G5** | Competing-school perspective is singular, but the case is a 4-way method bake-off. | **MINOR** | Instantiate the competing-schools perspective per method family in `planner.py` (Prompt 6); no rule change needed. |
| **G6** | User-specified section structure vs the derived outline. | **RESOLVED** | New `R-ARCH-07` + `user_structure` parameter + `Section.maps_to`: the user's structure governs top-level coverage and order, while headings stay predictive claims (the mapping is declared, not inferred, so R-SCENT-01 does not collide). |
| **G7** | `budget_weight` defaults read as prescriptive; this prompt inverts them (historian/bridge-builder high). | **MINOR (wording)** | Document defaults as *re-weightable priors* in `research-perspectives.md`; `planner.py` accepts overrides. |
| **G8** | Phase-1 research is the quality ceiling and the one thing a dry run cannot exercise. | **SCOPE NOTE** | Validate for real in Prompt 6/7 on `spec-01`/`spec-02`; the eval harness is where this gets measured. |
| **G9** | The domain's own figures are copyrighted. | **RESOLVED** | SKILL rendering rule: never fetch source-domain figures; draw synthetic SVG. |
| **G10** | `R-DISC-02` required "**each** discovery wave runs >= MIN_FRAMINGS (5) briefs spanning distinct cells … with >=1 orthogonal framing **per wave**", but `WAVE_ARCHETYPES` gives wave B three archetypes (**two** once `B-seed` skips without a seed) and wave C three — so every campaign ever run violated a MUST. | **RESOLVED** | Scoped to the **breadth wave (A)**. Three artifacts said the floor was about wave A: `MIN_FRAMINGS`' own comment ("Wave A breadth"), `references/discovery-brief-templates.md` (which makes B and C deliberately *narrow* follow-ups, and which `BRIEF_ARCHETYPES` is pinned to), and — decisively — the rule's own `counters`: *"cosmetically-different briefs pay Nx tokens for 1x recall"* is an argument about the broad sweep, where decorrelating the ensemble is the whole job. Forcing five cells onto a targeted follow-up would MANUFACTURE that padding. What every wave still owes is **distinctness** — duplicate cells are cosmetic in any wave — plus a `framing_cells` count matching the manifest. Directive, check, planner comment and test all updated in step; the real spec-02 campaign now passes R-DISC-02 clean. |
| **G11** | **`eval.py --strict` is RED.** The gate only ever consulted `expect.must_pass` — a spec's curated list of rules it wants exercised, six for spec-01 — never the registry's MUST set, so a critic MUST failure outside that list was invisible to every gate. Wiring it surfaced two on the reference artifact, both judged by **sonnet** on the gating tier and therefore not haiku variance: **R-ARCH-01** (the primer opens on a claim; there is no scope-and-decisions block) and **R-EVID-01** (`aid-reranking` and `fig-reranking` state precise thresholds — recall@k 0.8, 100 candidates — as flat fact under `Sources: [none yet — inferred]`, with no epistemic tag). | **RESOLVED** | Fixed in the artifact, which is what `eval-rubric.yaml` instructs (*"fix the artifacts, do not lower the bar"*), and landed in ONE pass with G12's re-judge and the per-row `row_claims` attribution, because editing the IR moves its digest and stales the report. The primer now opens on a scope-and-decisions block, and the two reranking blocks carry epistemic tags over their thresholds. Both rules PASS on the re-judged report, and so do R-EXPERT-02, R-EXPERT-03 and R-FIG-02[fig-reranking], which the same edits cleared. Coverage 46/79 → **79/79**. One MUST remains on spec-01 and it is not an artifact defect — see **G14**. |
| **G12** | **The frozen critic report is STALE, and the 79/79 coverage figure rested on it.** `critic-report.full.json` was judged against `document-ir.full.yaml` as of `49764e0` — identified by matching its original byte-stamp `e114186cc80d`. `f80ff65` then relabelled `matrix-chunking`, `body-chunk-size` and `toulmin-reranking` from `verified` to `inferred`, and kept the report credited by making the staleness digest blind to provenance, on the stated grounds that this was "a grounding correction the critics cannot see". **They can see it**: `_judge_document_view` builds the document unit from `render_llm_md`, which prints `provenance:` into every block heading, so three lines of the surface every document-level critic reads are different — and provenance is exactly what R-EVID-01 judges. With the digest keyed on the real judged surface and stamped to the document actually judged, the guard reported `stale`: soft_critic 2/35, total coverage 46/79. | **RESOLVED** | Re-judged, not re-stamped — re-stamping is what produced the problem, twice. 119 calls, ~$11.89-equivalent, ~40 min, haiku advisory + sonnet gating, against the IR at HEAD with G11's fixes already in it. The report's `ir_sha256` now equals `_ir_digest` of the IR shipped beside it, so `soft_critic` is **35/35** and every tier is at 100% against floors that were never lowered. Cheaper than the run it replaced despite 5% more calls ($17.85 → $11.89): `ClaudeCli` now passes `--tools ""` by default, and a critic judging a block of text needs no tools. The staleness guard keeps independent synthetic coverage in `test_eval.py` — the shipped report matching is not what proves the guard can still fail. |
| **G13** | **A third strict failure, undocumented until now: spec-02 grounds nothing.** All 13 of its claim-bearing blocks are `provenance: inferred`, so `ungrounded_share` is **1.00** against `max_inferred_share: 0.60` and `meets_composition` is false — `eval.py --strict` is red for this as well as for G11/G12. It is not new behaviour; it is newly *visible*. The composition guard used to sit behind the lexical-backend bypass, which made it unreachable in the only configuration that runs offline and in CI, so it could not fail and nothing was ever filed. Moving it out (it is computed from provenance tags and claim counts, no entailment backend involved) made it reachable, and the reference artifact is what it caught. §8 of `HANDOFF.md` shows the failure was foreseen and accepted without being recorded: spec-02 "has none [no verified spine], which is the honest verdict on a topic whose sources did not support it", and `0.60` is described there as "the one number here set by judgement rather than principle". | **OPEN — a decision about what the skill claims, not a bug** | The labelling is defensible on inspection: every source in `spec02/source-ledger.yaml` is a home-domain technique paper (TGGLines, ELSED, LETR, SAM) and every block's conclusion is a transfer to parts catalogues that no source states — which is what `inferred` means. But spec-02 is deliberately the `home ~= target` cross-domain stress case (**G1**), so a global 60% cap fails the skill's own flagship spec *by construction*. Three ways out, not equivalent: **(a) fix the artifact** — give spec-02 a verified spine from claims that do sit in their source's own domain (`C-lead-2`'s "other line detectors only retain a collection of loose line segments" is one), which is what `eval-rubric.yaml` instructs ("fix the artifacts, do not lower the bar") but means changing what the primer asserts; **(b) move the threshold** — cheapest and precisely the erosion the ratchet exists to catch, and there is no headroom either way: spec-03 sits *exactly* at 0.60 and passes only on `<=`; **(c) change what composition measures** — if a cross-domain primer's honest ceiling on `verified` is genuinely lower than a same-domain one's, the bound belongs per-spec or keyed on home/target distance, not as one global number. Current spread: spec-01 0.43 - spec-06 0.53 - spec-04 0.56 - spec-05 0.57 - spec-03 0.60 - **spec-02 1.00**. Surfaced rather than applied: picking one decides what deep-primer claims about its own flagship case. |
| **G14** | **The gating judge disagrees with itself ACROSS runs, and nothing can see it.** `R-SUMM-01` on `lede-reranking` passed in the `49764e0` run and failed in the re-judge, on input that did not change: the block's text is byte-identical between the two commits, and for a block-scoped rule the judged unit is only that block's `readable_text` plus `block_id/role/concept/mode` — no surrounding document — so the two prompts were byte-identical too. Sonnet gave *stable* (test-retest-agreeing) but opposite verdicts: "Complete claim about reranking's limit, not a topic announcement" → "States a fact/limitation, not a defended claim/thesis; reads as a definitional statement rather than a position". The test-retest control in `run_pass` judges each gating item twice **within one run**, so it is structurally blind to this: both runs were internally consistent. It is the sole remaining blocker on spec-01 and one of two reasons `eval.py --strict` exits 1. | **RESOLVED — (c)** | The lede is *not* edited, deliberately. It satisfies the rule's own written contrast pair (its negative example is a topic-label lede, "This section covers X"), so rewriting it would be optimising against noise — and it would stale a fresh $11.89 run to chase a verdict that may flip back. Three ways out, not equivalent: **(a) widen the control** — re-judge gating items across *sessions* rather than twice in one, which catches drift but multiplies the cost of the tier that already costs the most; **(b) treat a cross-run flip as `unstable`** — needs the previous report as an input to the next run, making the report a chain rather than a snapshot, and `unstable` does not block, so a genuinely-failing rule could hide there; **(c) accept single-verdict gating and record the flake rate** — cheapest, honest, and leaves a MUST rule gated on a coin with a known bias. Measured drift was 1 rule in 77 verdicts across two runs — too small a sample to pick from on the evidence, so it was decided rather than derived. **(c) is what shipped.** The first verdict gates; the second call is still made and is now recorded as `flipped`, and a run reports `flake` (`retested` / `flipped` / `flake_rate`) plus `flaky_items`. `unstable` is retired as an outcome: it blocked nothing and cleared nothing, so a rule stopped gating for exactly the documents it was least sure about, and the control was within-run anyway — blind to the drift it existed to catch. Spend is unchanged (the same two calls) and a number that accumulates across runs replaces a veto that fired on n=2. **This does not turn `eval.py --strict` green** and was not expected to: `R-SUMM-01` is now gated on one verdict from a judge measured to flip on it, so spec-01 goes red or green by run. The frozen `critic-report.full.json` is left exactly as judged — its one `unstable` (R-XREF-04, in no spec's `expect.must_pass`) still reads as `unstable`, because re-judging a paid report to restate its schema would spend money to learn nothing; `eval.py` keeps reading that verdict for legacy reports while new runs never emit it. |

**Status:** **G1, G2 and G6 applied** — G2 in Stage A, G1 and G6 as a registry step ahead of Stage C
(see `PLAN.md`). **G10 is RESOLVED** (opened when the discovery checks were first dispatched, closed by
scoping the framing floor to the breadth wave — see §8 of `HANDOFF.md`). **G11 and G12 are RESOLVED** — they were one spend
decision rather than two engineering problems, and a single re-judge cleared both, taking coverage to 79/79.
**G13 is the one OPEN gap.** `eval.py --strict` still exits 1, now for two reasons that are one decision and one
coin: **G13** is a judgement about the composition bound and about what spec-02 is allowed to claim, and
**G14**'s `R-SUMM-01` is now gated on a single verdict from a judge measured to flip on it — resolved as a
control, not as a green run. Neither is cleared by spending. Both were surfaced rather than quietly applied,
because both decide what deep-primer claims about itself; G14 was then decided by the maintainer. G5/G7 stay wording/implementation notes
handled in `planner.py` without rule changes, and G8 (Phase-1 research quality) is validated by the
eval harness rather than by a rule.

> **Note on the cost figure — a correction.** An earlier revision of this note claimed the committed
> report's `calls: 113, spend_usd: 17.85` was double the truth. **It is not.** That report was
> produced at `97ab26e`, where each `ClaudeCliJudge` owned an independent `ClaudeCli` carrying its
> own `spend_usd`/`calls`, so summing the two was correct then. The shared `Budget` arrived four
> commits later at `976db04`. The stamp is a true total, and `113` being ODD settles it on its own —
> a double count is `n + n`, always even.
>
> The double-count window was `976db04..abf363c` and no report was generated inside it. The code fix
> at `run_critics.main` (read the shared `Budget` once instead of summing both judges) is correct
> and stays; only the claim about the committed report was wrong.
>
> **The re-judge was ~$18-equivalent, not ~$9 — and came in at $11.89.** On a Claude MAX
> subscription that is not a bill: `claude -p` authenticates by OAuth and draws on subscription
> quota, and `total_cost_usd` is a notional API-equivalent. The binding constraint is rate limit,
> not money, which is why the per-call preamble saving below matters more than the dollar figure —
> and the actual run bears that out. It made 119 calls against the old report's 113 and still cost
> 33% less, because `ClaudeCli` now passes `--tools ""` by default: ~15.9k preamble tokens per
> cold session instead of ~37.8k, on a job where the judge only ever reads a block of text.
