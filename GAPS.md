# GAPS.md — open contract gaps from the dry run

These surfaced when the pipeline was traced against a hard prompt (a practitioner primer on reading callout/leader-line figures and segmenting exploded-view parts catalogues; reference case = a ~80-callout binary exploded view; thesis = anchors seed segmentation, masks may be unnecessary). `spec-02-callout-extraction.yaml` keeps this case live.

Apply any fix via the registry + `bash tools/build.sh` — **never** by hand-editing generated files.

| # | Gap | Status | Action |
|---|-----|--------|--------|
| **G1** | `home ≈ target`: reader already lives in the target domain, so the cross-domain bridge metaphor degenerates. | **RESOLVED** | New `R-XREF-04`: `home_anchor` resolves to the nearest adjacent *technique/sub-field*, with a deterministic companion lint (`home_anchor_distinct`) catching self-restatement; `research-perspectives.md` re-aims bridge-builder at prior-art transfer. |
| **G2** | `R-EXPERT-01` (suppress worked examples) collides with mandatory reference-case grounding. | **RESOLVED** | `R-EXPERT-01` narrowed to *procedural* walkthroughs; new `R-EXPERT-03` (running reference case) plus a `reference_case` parameter make the anchor first-class and explicitly not-suppressed. |
| **G3** | No consumer/dual-purpose knob (human reading vs LLM grounding). | **RESOLVED** | Became the dual-output decision: `outputs` param + `R-PROJ-01..06` (IR-first, two projections). |
| **G4** | Verified-vs-inferred provenance not surfaced. | **RESOLVED** | `R-PROJ-05` — provenance (`verified`/`inferred`/`unverified`) + `source_ids` printed inline in both projections; `provenance` axis added to the IR. |
| **G5** | Competing-school perspective is singular, but the case is a 4-way method bake-off. | **MINOR** | Instantiate the competing-schools perspective per method family in `planner.py` (Prompt 6); no rule change needed. |
| **G6** | User-specified section structure vs the derived outline. | **RESOLVED** | New `R-ARCH-07` + `user_structure` parameter + `Section.maps_to`: the user's structure governs top-level coverage and order, while headings stay predictive claims (the mapping is declared, not inferred, so R-SCENT-01 does not collide). |
| **G7** | `budget_weight` defaults read as prescriptive; this prompt inverts them (historian/bridge-builder high). | **MINOR (wording)** | Document defaults as *re-weightable priors* in `research-perspectives.md`; `planner.py` accepts overrides. |
| **G8** | Phase-1 research is the quality ceiling and the one thing a dry run cannot exercise. | **SCOPE NOTE** | Validate for real in Prompt 6/7 on `spec-01`/`spec-02`; the eval harness is where this gets measured. |
| **G9** | The domain's own figures are copyrighted. | **RESOLVED** | SKILL rendering rule: never fetch source-domain figures; draw synthetic SVG. |
| **G10** | `R-DISC-02` required "**each** discovery wave runs >= MIN_FRAMINGS (5) briefs spanning distinct cells … with >=1 orthogonal framing **per wave**", but `WAVE_ARCHETYPES` gives wave B three archetypes (**two** once `B-seed` skips without a seed) and wave C three — so every campaign ever run violated a MUST. | **RESOLVED** | Scoped to the **breadth wave (A)**. Three artifacts said the floor was about wave A: `MIN_FRAMINGS`' own comment ("Wave A breadth"), `references/discovery-brief-templates.md` (which makes B and C deliberately *narrow* follow-ups, and which `BRIEF_ARCHETYPES` is pinned to), and — decisively — the rule's own `counters`: *"cosmetically-different briefs pay Nx tokens for 1x recall"* is an argument about the broad sweep, where decorrelating the ensemble is the whole job. Forcing five cells onto a targeted follow-up would MANUFACTURE that padding. What every wave still owes is **distinctness** — duplicate cells are cosmetic in any wave — plus a `framing_cells` count matching the manifest. Directive, check, planner comment and test all updated in step; the real spec-02 campaign now passes R-DISC-02 clean. |
| **G11** | **`eval.py --strict` is RED.** The gate only ever consulted `expect.must_pass` — a spec's curated list of rules it wants exercised, six for spec-01 — never the registry's MUST set, so a critic MUST failure outside that list was invisible to every gate. Wiring it surfaced two on the reference artifact, both judged by **sonnet** on the gating tier and therefore not haiku variance: **R-ARCH-01** (the primer opens on a claim; there is no scope-and-decisions block) and **R-EVID-01** (`aid-reranking` and `fig-reranking` state precise thresholds — recall@k 0.8, 100 candidates — as flat fact under `Sources: [none yet — inferred]`, with no epistemic tag). | **RESOLVED** | Fixed in the artifact, which is what `eval-rubric.yaml` instructs (*"fix the artifacts, do not lower the bar"*), and landed in ONE pass with G12's re-judge and the per-row `row_claims` attribution, because editing the IR moves its digest and stales the report. The primer now opens on a scope-and-decisions block, and the two reranking blocks carry epistemic tags over their thresholds. Both rules PASS on the re-judged report, and so do R-EXPERT-02, R-EXPERT-03 and R-FIG-02[fig-reranking], which the same edits cleared. Coverage 46/79 → **79/79**. One MUST remains on spec-01 and it is not an artifact defect — see **G14**. |
| **G12** | **The frozen critic report is STALE, and the 79/79 coverage figure rested on it.** `critic-report.full.json` was judged against `document-ir.full.yaml` as of `49764e0` — identified by matching its original byte-stamp `e114186cc80d`. `f80ff65` then relabelled `matrix-chunking`, `body-chunk-size` and `toulmin-reranking` from `verified` to `inferred`, and kept the report credited by making the staleness digest blind to provenance, on the stated grounds that this was "a grounding correction the critics cannot see". **They can see it**: `_judge_document_view` builds the document unit from `render_llm_md`, which prints `provenance:` into every block heading, so three lines of the surface every document-level critic reads are different — and provenance is exactly what R-EVID-01 judges. With the digest keyed on the real judged surface and stamped to the document actually judged, the guard reported `stale`: soft_critic 2/35, total coverage 46/79. | **RESOLVED** | Re-judged, not re-stamped — re-stamping is what produced the problem, twice. 119 calls, ~$11.89-equivalent, ~40 min, haiku advisory + sonnet gating, against the IR at HEAD with G11's fixes already in it. The report's `ir_sha256` now equals `_ir_digest` of the IR shipped beside it, so `soft_critic` is **35/35** and every tier is at 100% against floors that were never lowered. Cheaper than the run it replaced despite 5% more calls ($17.85 → $11.89): `ClaudeCli` now passes `--tools ""` by default, and a critic judging a block of text needs no tools. The staleness guard keeps independent synthetic coverage in `test_eval.py` — the shipped report matching is not what proves the guard can still fail. |
| **G13** | **A third strict failure, undocumented until now: spec-02 grounds nothing.** All 13 of its claim-bearing blocks are `provenance: inferred`, so `ungrounded_share` is **1.00** against `max_inferred_share: 0.60` and `meets_composition` is false — `eval.py --strict` is red for this as well as for G11/G12. It is not new behaviour; it is newly *visible*. The composition guard used to sit behind the lexical-backend bypass, which made it unreachable in the only configuration that runs offline and in CI, so it could not fail and nothing was ever filed. Moving it out (it is computed from provenance tags and claim counts, no entailment backend involved) made it reachable, and the reference artifact is what it caught. §8 of `HANDOFF.md` shows the failure was foreseen and accepted without being recorded: spec-02 "has none [no verified spine], which is the honest verdict on a topic whose sources did not support it", and `0.60` is described there as "the one number here set by judgement rather than principle". | **ROUTE IMPLEMENTED (2026-08-21) — (c) via (i); spec-02 still fails, on purpose** | The labelling is defensible on inspection: every source in `spec02/source-ledger.yaml` is a home-domain technique paper (TGGLines, ELSED, LETR, SAM) and every block's conclusion is a transfer to parts catalogues that no source states — which is what `inferred` means. But spec-02 is deliberately the `home ~= target` cross-domain stress case (**G1**), so a global 60% cap fails the skill's own flagship spec *by construction*. Three ways out, not equivalent: **(a) fix the artifact** — give spec-02 a verified spine from claims that do sit in their source's own domain (`C-lead-2`'s "other line detectors only retain a collection of loose line segments" is one), which is what `eval-rubric.yaml` instructs ("fix the artifacts, do not lower the bar") but means changing what the primer asserts; **(b) move the threshold** — cheapest and precisely the erosion the ratchet exists to catch, and when this was written there was no headroom either way — spec-03 sat *exactly* at 0.60 and passed only on `<=`. Real grounding has since moved it: `301b61f` re-drafted spec-03's claim-bearing blocks against its campaign ledger and it now scores **0.40** (9 `verified` / 6 `inferred`), so 0.20 of headroom appeared purely from grounding the artifact rather than from touching the number; **(c) change what composition measures** — if a cross-domain primer's honest ceiling on `verified` is genuinely lower than a same-domain one's, the bound belongs per-spec or keyed on home/target distance, not as one global number. Final spread, all four grounding campaigns now landed (2026-08-21): spec-03 **0.40** - spec-01 0.43 - spec-04 **0.44** - spec-06 **0.45** - spec-05 **0.57** - **spec-02 1.00**. Three of the four re-drafted specs moved DOWN and one did not move at all - spec-03 0.60 to 0.40, spec-04 0.56 to 0.44, spec-06 0.53 to 0.45, spec-05 0.57 to 0.57 - so grounding has produced every point of headroom that exists, but it does not produce headroom reliably. spec-05 is the instructive one: its re-draft replaced every synthetic claim with a real one and kept the same 9-verified / 12-inferred split, because what a cross-domain primer *asserts* is a drafting decision that real sources constrain but do not determine. The cap is therefore being fitted against a spread whose lowest and highest values (0.40 and 1.00) are both real-campaign artifacts. Surfaced rather than applied: picking one decides what deep-primer claims about its own flagship case. **The maintainer chose (c)** — *"make the cap depend on the type of primer instead of one global number"*. What (c) does not yet have is a definition of *type*: `R-PARAM-01` sets four parameters (`home_domain`, `target_domain`, `seniority_band`, `length_budget`), with `outputs` defaulted and `user_structure` / `reference_case` / `seed_sources` optional, and none of the eight is one. Two ways to supply it, not equivalent: **(i) an eighth explicit `primer_type`** — auditable, states the bargain up front, and enumerable in the registry, but it is a knob the caller sets, and the caller is the party who wants the primer to pass; **(ii) derive it from home↔target distance** — no new parameter, and it keys the bound to the thing actually moving it, since spec-02 is both the `home ≈ target` stress case and the 1.00 outlier, but the skill makes no such measurement today (`R-XREF-04`'s `home_anchor_distinct` lint is the nearest thing). **Two things measured on 2026-08-21 narrow this.** First, neither parameter that already exists can serve as the type: `seniority_band` does not separate the spread at all — `staff_plus` spans 0.43 / 0.57 / 1.00 and `mid_senior` spans 0.40 / 0.44 / 0.53, so the two ranges interleave with the lowest and the highest value in different bands — and `length_budget` is not monotone in it either (2500→0.40, 4000→0.44, 4500→0.53, 5000→0.57, 6000→0.43, 8000→1.00). So (i)'s new parameter is not avoidable by re-using an old one. Second, *distance* may be the wrong axis for the outlier it was chosen to explain: spec-02's home (`classical CV`, `OCR`, `instance segmentation`, `SAM family`, `document-AI`) and its target (callout reading and exploded-view segmentation) are the *same* field — what its target lacks is not proximity to home but a literature of its own, and its ledger shows it (4 sources, 7 claims, 0 corroborated, against 30 / 281 / 22 for a real spec-03 campaign). That points at a third option not previously listed: **(iii) derive the cap from what the campaign actually retrieved** — corroboration rate and source count per concept are already computed, they measure the thing that makes grounding possible rather than a proxy for it, and unlike (i) they are not set by the party who wants the primer to pass. Also worth noting before any fitting: `ungrounded_share` and `inferred_share` are numerically identical on all six specs today, so the wider population the cap is deliberately measured against (`unverified` and untagged blocks) is exercised by nothing in the corpus. The NUMBERS are still not fitted, but the precondition is now met: the sequencing decision was that real grounding campaigns for specs 03–06 run first so the cap is fitted once against ledgers built from real sources, and all four have landed (`301b61f`/`6233997` spec-03, `34a376d`/`08d3495` spec-04, `1912684` spec-05, `30c3ec9` spec-06). That was not a formality — re-grounding spec-03 through the fixed pipeline took it from 260 concepts to 19 and from 0 corroborated claims to 22, so a threshold fitted to the pre-campaign spread would have been fitted to the word-overlap grouper. What remains blocking is the taxonomy, not the data: options (i) an explicit `primer_type`, (ii) home↔target distance and (iii) a cap derived from what the campaign retrieved are still live, and the six-point spread does not choose between them on its own. That choice is the maintainer's and is deliberately not being made here. **Resolved 2026-08-21 — route (c) implemented, with the taxonomy supplied by (i).** The maintainer's wording chose between the three suppliers on its own: *"make the cap depend on the **type of primer** instead of one global number"* is an explicit `primer_type`, not home↔target distance (ii) and not campaign yield (iii). Composition is not a registry rule — `max_inferred_share` lives inside the model_verified tier's verifier — so this needed no registry edit and no `build.sh` regeneration. `eval-rubric.yaml` now carries `composition_by_primer_type` with **two** entries and `default_primer_type: survey`; each spec declares `primer_type` beside `target_domain`. **Each entry is a pair, not a cap.** Measuring `ungrounded_share` alone measures *apparatus density* as much as grounding: spec-05 scores worst of the five compliant specs (0.57) while having a fully grounded spine, because it carries more matrix/figure/body blocks that are legitimately synthesis — i.e. a primer is penalised for being thorough. So each type pairs a `max_ungrounded_share` with a `min_spine_grounded` floor over every `lede`/`card` block in the document: **survey 0.60 / 0.90**, **frontier 0.85 / 0.50**. The spine is counted over the whole document rather than over the composition-scoped set, which closes a strip-the-tags evasion and is numerically identical on all six specs today; a document with no lede or card at all is *vacuous* on the spine (non-blocking), because `R-ARCH-06` already fails structural absence deterministically and the citation verifier must not re-litigate it. An **unrecognised** `primer_type` resolves to the strictest row in the table, never to the default, so a typo in a spec can never loosen a gate; and `propose_thresholds` now **excludes specs that currently fail** from its fit, otherwise a min-0.05 fit over spec-02 (ungrounded 1.00 / spine 0.00) would propose a bound that cannot fail — deriving a threshold from the artifact it exists to catch. **Neither type admits spec-02, and that is the point.** Under `frontier` its cap rises 0.60 → 0.85 and it still fails at 1.00, and its spine is **0/4** against a floor of 0.50. Its own ledger holds 13 home-domain technique papers, so its four ledes and cards are citable; 0/4 is an artifact defect under any type. The pin `test_spec_02_grounds_nothing_and_the_composition_gate_says_so` in `test_eval.py` **was moved on purpose** — it now asserts `primer_type == "frontier"` and `max_inferred_share == 0.85` — and `meets_composition` deliberately stays `False`. That is not the ratchet eroding: the cap loosened by 0.25 and the artifact still fails, because a second gate was added beside it. Two types and not three: a third would be invented from zero observations. What is still open is narrower than what G13 opened with. `eval.py --strict` remains red for G13, but no longer because one global number fails the flagship spec *by construction* — now because spec-02's spine cites nothing while its ledger can support it. Fix the artifact. |
| **G14** | **The gating judge disagrees with itself ACROSS runs, and nothing can see it.** `R-SUMM-01` on `lede-reranking` passed in the `49764e0` run and failed in the re-judge, on input that did not change: the block's text is byte-identical between the two commits, and for a block-scoped rule the judged unit is only that block's `readable_text` plus `block_id/role/concept/mode` — no surrounding document — so the two prompts were byte-identical too. Sonnet gave *stable* (test-retest-agreeing) but opposite verdicts: "Complete claim about reranking's limit, not a topic announcement" → "States a fact/limitation, not a defended claim/thesis; reads as a definitional statement rather than a position". The test-retest control in `run_pass` judges each gating item twice **within one run**, so it is structurally blind to this: both runs were internally consistent. It is the sole remaining blocker on spec-01 and one of two reasons `eval.py --strict` exits 1. | **RESOLVED — (c)** | The lede is *not* edited, deliberately. It satisfies the rule's own written contrast pair (its negative example is a topic-label lede, "This section covers X"), so rewriting it would be optimising against noise — and it would stale a fresh $11.89 run to chase a verdict that may flip back. Three ways out, not equivalent: **(a) widen the control** — re-judge gating items across *sessions* rather than twice in one, which catches drift but multiplies the cost of the tier that already costs the most; **(b) treat a cross-run flip as `unstable`** — needs the previous report as an input to the next run, making the report a chain rather than a snapshot, and `unstable` does not block, so a genuinely-failing rule could hide there; **(c) accept single-verdict gating and record the flake rate** — cheapest, honest, and leaves a MUST rule gated on a coin with a known bias. Measured drift was 1 rule in 77 verdicts across two runs — too small a sample to pick from on the evidence, so it was decided rather than derived. **(c) is what shipped.** The first verdict gates; the second call is still made and is now recorded as `flipped`, and a run reports `flake` (`retested` / `flipped` / `flake_rate`) plus `flaky_items`. `unstable` is retired as an outcome: it blocked nothing and cleared nothing, so a rule stopped gating for exactly the documents it was least sure about, and the control was within-run anyway — blind to the drift it existed to catch. Spend is unchanged (the same two calls) and a number that accumulates across runs replaces a veto that fired on n=2. **This does not turn `eval.py --strict` green** and was not expected to: `R-SUMM-01` is now gated on one verdict from a judge measured to flip on it, so spec-01 goes red or green by run. The frozen `critic-report.full.json` is left exactly as judged — its one `unstable` (R-XREF-04, in no spec's `expect.must_pass`) still reads as `unstable`, because re-judging a paid report to restate its schema would spend money to learn nothing; `eval.py` keeps reading that verdict for legacy reports while new runs never emit it. |
| **G15** | **`R-DISC-06` has never been exercised by a real campaign — the rule whose entire subject is user seeds.** Three independent breaks on the same path, found while preparing spec-04's grounded fixture for the re-draft. Two are bugs and are FIXED: (1) `run_campaign._spec_params` returned `spec["parameters"]` alone, but `seed_sources` is a **top-level** spec key (`artifact-schemas.md` files it under "Phase 0" and spec-04 declares it beside `parameters`), while `front_load_campaign` reads it back out of the params dict — so every campaign ever run resolved its seeds to `[]`; this is the *same* bug already fixed on the eval path, in the second place that reads a spec. (2) `front_load_campaign` did `seed_direct, _ = route_seeds(...)` and dropped the directive half one line later, so an `author`/`entity` seed never set `params["seed"]` and `wave_briefs` skipped the `B-seed` brief — the only thing that ever produced one was a test setting `params["seed"]` by hand, which is precisely why no test caught it. Both now carry mutation-verified tests. The measurement that exposed it: spec-04's grounded run produced **155 source leads, none `provenance_origin: user`**, 0 contested claims and 0 `contradicts`, against a spec whose `expect.must_pass` names `R-DISC-06` and whose `watch` list is `[seed_not_dropped, seed_not_promoted_past_evidence]`. | **PARTLY RESOLVED — two bugs fixed, two decisions open** | What the fixes do **not** buy is a fixture that exercises the rule, for two reasons that are decisions rather than defects. **(a) The discovery `Judge` seam has no live implementation.** `planner.Judge` is a Protocol with exactly one implementor, `StubJudge`, whose own docstring says it "is not a substitute for one" — and `run_campaign` injects a model at the *structure* seam (`ClaudeStructureJudge`) while passing no judge at the *discovery* seam, so every real campaign has run the stub. Two consequences are already visible in the committed fixtures: `extract_leads` harvests topic leads by scraping `- lead:` marker lines that live research reports do not emit, so spec-03 (143 source leads) and spec-04 (155) both carry **0 topic_leads**; and `triage` returns `accepted` for every cluster, so the only drop a real campaign performs is `triage_leads`' fetch-failure short-circuit. Writing a `ClaudeDiscoveryJudge` is the honest fix; it is also a new model seam with its own cost, and it changes what every grounded fixture contains. **(b) spec-04's seed URL is a placeholder that cannot ground.** `https://example.org/hnsw-paper` does not fetch, and `triage_leads` checks the fetch-failure short-circuit *before* the R-DISC-06 exemption on purpose ("a seed that will not load is still not evidence"), so `checks/discovery.seed_handling` fails by construction on any real run of that spec no matter what the pipeline does. The paper the placeholder stands for is already **source #1 of spec-04's own grounded ledger** — `https://arxiv.org/abs/1603.09320`, the Malkov & Yashunin HNSW paper, 8 claims, which arrived through ordinary discovery rather than as a seed. Pointing the spec at the real URL is what would make the spec do the job its header claims ("Exercises seed_sources end-to-end"), but it changes what the spec tests and must move four files in lockstep (the spec plus `spec04/{discovery-leads,source-ledger,document-ir}.yaml`, where the synthetic fixture hand-supplies the `sl-seed-hnsw` lead that makes R-DISC-06 pass today). Surfaced rather than applied: (a) decides how much of discovery is model-driven, and (b) decides whether a spec's seed is a stand-in or a real source. **(b) is now DECIDED and APPLIED (`08d3495`): the spec points at `https://arxiv.org/abs/1603.09320`.** The reason it could not be deferred is that the ordering in `triage_leads` makes the placeholder *unfailable in the direction that matters* — the seed was dropped for fetch failure before R-DISC-06 could exempt it, so the rule reported green against a hand-built artifact and could not have run at all against a campaign. spec-04's five artifacts were rebuilt on the 2026-08-21 run in lockstep and each records, in its own header, which fields came from the run and which are authored. **(a) stays open**, and G16 is the same seam problem one stage later. |
| **G16** | **No campaign has ever marked a conflict, so the `contested` half of `R-GROUND-05` passes vacuously on every real run — and `R-DISC-06`, a MUST, delegates its grading to it.** `claim_extractor.mark_conflicts` exists, records disagreement symmetrically, and has a mutation-worthy unit test (`test_mark_conflicts_is_symmetric`). It takes a **model-supplied** `contradictions` dict, and nothing supplies one: `run_campaign.py:40` imports `build_ledger`, `corroborate` and `mark_recency` and not `mark_conflicts`, and the only other reference to the function in the whole skill is that test. Measured across the three campaigns run so far — spec-03 (281 claims), spec-04 (333), spec-05 (283), **897 claims — `contested` is 0 and `contradicts` is empty everywhere**. This is not a corpus that happens to agree with itself; it is a field no code path writes. Corroboration is the control that proves the point: the same stage sets it on 22 / 20 / 21 claims respectively, because `_corroborate_by_group` **does** have a live supplier (`ClaudeCorroborationGrouper`). The consequence is that `checks/ledger.py::provenance_fields` — which only checks that `contested` and `contradicts` travel together and resolve to claims in the ledger — has nothing to check, so half of R-GROUND-05 cannot fail on a real ledger. R-DISC-06 (MUST) then inherits the hole: its directive says a seed *"contradicted by the discovered consensus renders as contested"*, which is a grading rule resting on a signal no campaign produces. Both contested pairs in `tests/fixtures/spec04/source-ledger.yaml` are authored for this reason, and its header now says so. | **OPEN — surfaced, not applied** | Three ways out, not equivalent. **(a) A `ClaudeContradictionDetector` at the extraction seam**, mirroring `ClaudeCorroborationGrouper` exactly — honest and symmetric with how corroboration was solved, but it is a fourth model seam with its own cost, and contradiction over N claims is pairwise, so it needs the same propose-then-resolve trick or it is O(n^2) in claims (897 claims across three runs is ~400k pairs). **(b) Derive candidate conflicts from the corroboration groups already computed** — the grouper has already decided which claims assert one fact, so a second cheap pass over each surviving group asking only *"do these agree?"* reuses a call that was already paid for. Its limit is structural and should be stated rather than discovered: it can only find disagreement between claims similar enough to have been grouped, which misses the conflict that matters most — two sources framing a question so differently that no grouper puts them together. **(c) Accept that `contested` is an authoring-time signal, not a retrieval-time one**, and say so in the registry — cheapest, and it stops a rule reporting green on evidence it never had, but it demotes conflict detection from something the pipeline does to something a human does, which is a real reduction in what the skill claims. **(b) is the cheapest honest option** and is what I would pick, but picking it decides how much of the epistemic work deep-primer does for itself, so it is recorded here rather than applied. |
| **G17** | **Retrieval accepts anything that returns bytes, so bot walls and landing pages enter the ledger as sources.** `HttpFetcher.__call__` (`scripts/research/http_fetcher.py:153-155`) refuses a fetch on exactly one content condition — `not text.strip()`. `fetch_source_leads` (`scripts/research/retrieval_loop.py:153-157`) refuses on exactly one condition — `doc is None`. Nothing between them asks whether the body is the document that was sought. Two failure classes, both present in spec-05's real campaign output. **(1) Walls — wholly false attribution.** 37 of 131 retrieved documents are Cloudflare "Client Challenge" interstitials and 7 more are under 60 words (PubMed cookie notices, PMC reCAPTCHA stubs): **34% of the corpus is not a document**. Six of those sources reached the ledger carrying 17 claims that are the interstitial's own troubleshooting text — `text: Ad blockers can interfere with website functionality`, `quote: disable any ad blockers`, attributed to `trialsjournal.biomedcentral.com` and typed `primary_paper`. The type is inherited, never observed: `doc.source_type = doc.source_type or lead.type` takes discovery's pre-fetch guess, so a wall is stamped with the highest credibility tier its lead was assigned. **(2) Landing pages — a silent depth downgrade, and not length-detectable.** The FDA adaptive-design guidance resolves to its 262-word landing page rather than the guidance; EMA ICH E20 to 241 words; EMA methodological issues to 222. These are extracted faithfully and yield 23 claims that mix real abstract-level content ("The guideline provides guidance on adaptive designs for confirmatory clinical trials") with bibliographic furniture presented as finding ("The document was last updated on November 29, 2019"; "The current version has reference number CHMP/EWP/2459/02"). All are typed `standard` and all would resolve cleanly through `resolves_to_ledger` — the primer loses the whole substance of the guidance while every grounding check stays green. **Blast radius on committed work is nil.** spec-03's raw run has 2 such claims, spec-04's has 0, and reading every claim of both committed *curated* fixtures (`spec03/source-ledger.yaml`, 13 sources; `spec04/source-ledger.yaml`, 6) finds no furniture at all — hand-curation caught it. But curation is not part of the skill's contract, so nothing in the pipeline caught it. Three options, none applied: **(a)** a minimum-content gate in `HttpFetcher` (refuse below ~N words) — nearly free, removes the entire wall class, blind to class 2, and needs care not to drop legitimately short primary sources such as a spec erratum; **(b)** an interstitial fingerprint list — precise and nameable, needs maintenance as providers reword, still blind to class 2; **(c)** a model-side "is this the document you asked for?" check at extraction — the only one of the three that can see class 2, at the cost of a fifth model seam and one call per retrieved document. **(d)** — found after this row was first written, in spec-05's own concept map — **the grouper is already a detector and its answer is already discarded**: 13 of the map's 30 singletons are verbatim wall text, singletons precisely because nothing real in a 131-document corpus groups with them, and the grouper could not even name them (`settings browser`, `requires content`, `extensions interfere`). Singleton status plus a degenerate canonical term costs zero extra calls because the grouping already runs. It is blind to class 2 for a structural reason worth stating: the FDA and EMA furniture claims group **cleanly**, into `Adaptive trial regulatory guidance`, the map's largest concept at 37 claims, sitting beside the real ones — a page that is genuinely about the topic but is not the document produces claims that are genuinely about the topic. **(3) Worked-example contamination — a third class, found in spec-06's campaign on 2026-08-21, and the hardest of the three.** Source `8ee2ad4d` in that run is the RAGAS paper (arXiv 2309.15217v1): genuine, primary, exactly on topic, fetched in full. **8 of the 24 claims extracted from it are facts about the 2023 film *Oppenheimer* and the Chimnabai Clock Tower in Vadodara** — the WikiEval passages RAGAS prints as worked examples of its own metrics. Every signal that exists reports green: the document is long, it is not an interstitial, it is not a landing page, and it *is* the document that was asked for. Neither a length gate (a), nor a fingerprint list (b), nor a model-side "is this the document you asked for?" check (c) can catch it, because all three judge the DOCUMENT and the document is correct — the confusion is inside it, at the claim level. The only signals that could see it are claim-level: a claim whose entities share nothing with the target domain, or a concept group that is coherent, sizeable and about the wrong subject. Curation caught these too (the committed `spec06/source-ledger.yaml` carries none of them), which is the same non-guarantee as before. **(a) plus (d) is my pick for the wall class** — both nearly free — **class 2 needs (c) or nothing**, since it is invisible to every signal that does not compare the retrieved page against what was asked for, **and class 3 needs a claim-level relevance signal that does not exist anywhere in the pipeline today**. | **OPEN — surfaced, not applied** | Measured, not applied. Nothing already committed is affected; must be settled before any campaign's output is trusted without hand-curation. |

**Status:** **G1, G2 and G6 applied** — G2 in Stage A, G1 and G6 as a registry step ahead of Stage C
(see `PLAN.md`). **G10 is RESOLVED** (opened when the discovery checks were first dispatched, closed by
scoping the framing floor to the breadth wave — see §8 of `HANDOFF.md`). **G11 and G12 are RESOLVED** — they were one spend
decision rather than two engineering problems, and a single re-judge cleared both, taking coverage to 79/79.
**G13, G15, G16 and G17 are the gaps still open** — G13's route, taxonomy and numbers are now all decided and implemented (an explicit `primer_type`, two types, each a cap paired with a spine floor, fitted behind the four real grounding campaigns for specs 03–06 that it was sequenced behind), and what remains is not a design question but spec-02's ungrounded spine; G15's two bugs are fixed, its seed decision is taken and applied, and the live discovery judge is not; G16 is that same missing-supplier shape one stage later, at conflict detection, and is untouched; G17 is a different shape from all three — not a seam with no supplier but a gate that was never written, and the only one of the four that puts positively wrong text into a ledger rather than leaving a signal blank. `eval.py --strict` still exits 1, now for two reasons that are one artifact defect and one
coin: **G13**'s bound is now settled and per-type, and it is red only on the artifact it was always about — spec-02's four ledes and cards cite nothing — and
**G14**'s `R-SUMM-01` is now gated on a single verdict from a judge measured to flip on it — resolved as a
control, not as a green run. Neither is cleared by spending. All were surfaced rather than quietly applied,
because each decides what deep-primer claims about itself; G14 was then decided by the maintainer. G15 does not move `--strict` either way today: the rule it concerns has been reported as passing on a spec whose seed never reached the pipeline, which is the sharper version of the same problem G11 opened. G5/G7 stay wording/implementation notes
handled in `planner.py` without rule changes, and G8 (Phase-1 research quality) is validated by the
eval harness rather than by a rule.

> **Note on the cost figure — a correction.** An earlier revision of this note claimed the committed
> report's `calls: 113, spend_usd: 17.85` was double the truth. **It is not.** That report was
> produced at `97ab26e`, where each `ClaudeCliJudge` owned an independent `ClaudeCli` carrying its
> own `spend_usd`/`calls`, so summing the two was correct then. The shared `Budget` arrived four
> commits later at `976db04`. The stamp is a true total, and `113` being ODD settles it on its own —
> a double count is `n + n`, always even.
>
> The double-count window was `976db04..abf363c` and no report was generated inside it. The code fix
> at `run_critics.main` (read the shared `Budget` once instead of summing both judges) is correct
> and stays; only the claim about the committed report was wrong.
>
> **The re-judge was ~$18-equivalent, not ~$9 — and came in at $11.89.** On a Claude MAX
> subscription that is not a bill: `claude -p` authenticates by OAuth and draws on subscription
> quota, and `total_cost_usd` is a notional API-equivalent. The binding constraint is rate limit,
> not money, which is why the per-call preamble saving below matters more than the dollar figure —
> and the actual run bears that out. It made 119 calls against the old report's 113 and still cost
> 33% less, because `ClaudeCli` now passes `--tools ""` by default: ~15.9k preamble tokens per
> cold session instead of ~37.8k, on a job where the judge only ever reads a block of text.
