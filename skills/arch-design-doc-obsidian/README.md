# arch-design-doc-obsidian (placeholder)
Drop your existing skill folder here (SKILL.md + references/ + …). Keep it self-contained;
if it grows to share code with deep-primer, promote that code to ../../shared/ and rely on the bundle step.
