# Eval specs (deep-primer)

Per-skill eval lives here; the shared runner is `../../../eval/` (repo root) invoked via `scripts/eval.py`.
Specs are parameterized primer requests; `eval-rubric.yaml` is the scoring overlay (hard lints + model_verified + human).
Add 5–8 specs spanning domains / seniority bands / lengths before running the skill-creator loop (Prompt 7).
